"use strict"; //treat all JS code as newer version

// alert("hello") will give error because we are not running it on browser

let name="yash";
let age=23;
let isLoggedIn=false;
 
// number=>2 to the power 53
// bigint 
// string=> ""
// boolean=> true/false;
// null => stand alone value (representation of empty value,khali hai)
// undefined => value is not assigned (abhi defined nhi hai)
// symbol => unique 
// object (later)

console.log(typeof("2334")); //string
console.log(typeof null); //object;
console.log(typeof undefined); //undefined;